@component('layouts/main')

@slot('title')
<span class="text-blue-700 font-bold text-2xl flex items-center gap-2">
  📘 {{ tip.title }} – AquaSentinel
</span>
@endslot

<div class="max-w-3xl mx-auto px-6 py-12 space-y-6">
  <h1 class="text-3xl font-extrabold text-gray-800">{{ tip.title }}</h1>

  <p class="text-gray-600 text-sm">By {{ tip.user.username }} | Linked Forecast: {{ tip.forecast?.title || 'N/A' }}</p>

  <div class="prose max-w-none text-gray-800 mt-4">
    {{ tip.content }}
  </div>

  <div class="mt-8">
    <a href="/educational_contents/category/{{ tip.category }}" class="text-blue-600 underline"> ← Back to {{ tip.category }} tips</a>
  </div>
</div>

@endcomponent
